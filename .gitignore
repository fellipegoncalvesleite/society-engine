# Dependencies and build output
node_modules/
dist/

# Generated benchmark/audit artifacts
artifacts/
docs/baselines/
timing_audit.txt

# Local working notes — not part of the published project
PRODUCT.md
DESIGN.md
# AI-agent handoffs, diagnostics, and planning scratch files
**/HANDOFF.md
**/HANDOFF-*.md
**/*_HANDOFF.md
**/*_DIAGNOSTIC.md
docs/superpowers/

# Local assistant/tool cache (design/config files remain tracked)
.impeccable/hook.cache.json
# Agent-harness dev-server config. CORRECTION-23G §4 removed a .claude/launch.json that the
# diagnostic workflow had committed by accident; it configures a local preview server and has
# nothing to do with the simulator. Ignored so it cannot silently return.
.claude/
