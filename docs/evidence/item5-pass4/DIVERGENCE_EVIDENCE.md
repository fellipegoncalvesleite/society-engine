# Roadmap Item 5 Pass 4 — Divergence Evidence

Status: deterministic causal-divergence record. A changed whole-world fingerprint is treated as evidence to explain, not a target to tune back to.

## Final exact-tree determinism

The final closure-candidate source bytes are deterministic under repeated and fresh-process execution. `demographicLongRunAudit.mjs --map map2 --years 50 --repeat` reports repeat-identical state and fingerprint:

`2f4b6b3a13b78f6b30555d0b503c50eb308aa10d59e496b4561bc7edb3f34287`

Two additional independent Node processes produced the same fingerprint and the same key outcomes (`endPopulation=214`, `activeBands=9`, `movements=1077`, `adaptationsAttempted=8`, `adaptationsEffective=3`). The final value therefore reproduced across four executions, including independent processes.

An earlier Pass-4 evidence capture recorded:

`1d4424667b4047a705877dda15befff86148cb50ccc8e92b5276fa5156d24744`

That earlier value is not reproducible from the final exact source bytes. The same pre-final evidence set also contained natural-run counts, canonical Item-5 digests, and reversible-mutation source hashes that differ from the final exact-tree reruns. The evidence was therefore stale with respect to the final source state. It is superseded rather than treated as a target to restore.

The accepted Pass-3 Map-2 fingerprint was:

`f509b2f4d6e8a463b7505025afe22d151fd45a0598bd9550fbb84aac900da03c`

The final Pass-4 and accepted Pass-3 fingerprints differ. No coefficient was retuned to restore either the Pass-3 value or any stale intermediate Pass-4 value. The final fingerprint also differs from the pre-cooldown closure-correction fingerprint because certification identified and corrected immediate blocked-plan cycling; population, movement, and effective-adaptation outcomes remain stable.

## First causal divergence from accepted Pass 3

A cross-worktree comparison was run between the accepted Pass-3 tree and the Pass-4 tree using the same Map-2 seed (`demographic-long-run:map2`) and deterministic stepping.

The first full dynamic-snapshot divergence occurs at season 1. At that point the relevant difference is Pass-4 epistemic state: selected bands have weak `encountered_woody_plant` `HumanMaterialBelief` records. Population and the comparison projection with `practicalAdaptation` / `adaptiveHuman` removed remain identical.

The comparison with Item-5 state removed remains identical through season 26. The first divergence in that reduced snapshot occurs at season 27, after the Pass-4 compositional invention path has selected different practice-only ideas/history than the Pass-3 catalog-exclusive path.

A representative divergence is a band for which Pass 3 selected catalog material responses while Pass 4 held local material beliefs, selected a practice-only staged-burden design, and admitted a non-template shelter hypothesis without manufacturing material execution. That is exactly the architecture Pass 4 is intended to introduce.

Classification:

**EXPECTED PASS-4 INVENTION-SEMANTIC CHANGE**

It is not classified as an Item-4, ecology, demography, or physical-coefficient regression.

## Controlled divergence A–P

Fresh `item5Pass4CompositionalAudit.mjs` result on the final exact source bytes: **PASS, 31/31 checks**. The original A–P cases remain green and closure assertions Q/R/S/T additionally prove primitive composition, temporal material actionability, localized failed-binding substitution, and truthful blocked-plan lifecycle. The strengthened T check also proves the bounded 32-tick blocked-plan reconsideration memory expires and does not suppress an executable historical variant. The controlled suite preserves these original cases:

- A — different human-known environments produce different candidate possibilities while hidden truth alone does not;
- B — same environment plus different technical history diverges;
- C — convergent function can retain different normalized designs/history;
- D — failure changes later revision path without deleting the functional family;
- E — daughter inheritance is degraded hints/knowledge only;
- F — no new canonical diffusion writer is fabricated;
- G — knowledge persists across environmental change while local reproducibility can lapse;
- H — independent discovery may share a signature while history stays distinct;
- I — same state/seed replays identically;
- J — candidate/store budgets remain bounded;
- K — semantically equivalent reordered designs normalize to the same signature;
- L — causal design differences retain different signatures;
- M — known joint failure does not damage independent demonstrated edge knowledge;
- N — ambiguous failure does not invent precise blame;
- O — unsupported material-required design produces zero fabricated physical effect/efficacy;
- P — hidden world changes with identical human knowledge do not change candidate generation.

Current controlled values include normalized equivalence signature `design:bf976a52`, fiber-opportunity candidate digest `928b6f3c1ca047e8ff2a32ca20aa9093bd3bc82cc1c7ed6dcf9457b47d71bb5e`, and stone-opportunity candidate digest `67d039d2a3f877c33493c7ea24138c0e6859cd0eae117b5c1439fa6ccc94771e`.

## Natural divergence / non-vacuity

Fresh final-tree natural runs show named-template and non-template invention activity and independent convergence. The 120-year Map-2 arm also naturally produces revision ideas, partial outcomes, abandoned experiments, dead-end lessons, and a dormant lesson. Features absent in the tested arms, such as reactivation at these declared seeds/horizons, are explicitly reported as not naturally observed rather than fabricated.

Exact natural measurements are reported in `CAPS_AND_PERFORMANCE.md` and `PASS4_EVIDENCE.md`.
